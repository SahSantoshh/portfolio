---
import TechBadge from "./TechBadge.astro";

interface Props {
  name: string;
  tagline: string;
  description: string;
  url: string;
  icon?: string;
  playStoreUrl?: string;
  playStoreIcon?: string;
  stack: string[];
  featured?: boolean;
}

const { name, tagline, description, url, icon, playStoreUrl, playStoreIcon, stack, featured = false } =
  Astro.props;
---

<article
  class:list={[
    "group flex flex-col rounded-2xl border p-6 transition sm:p-8",
    featured
      ? "border-ruby/35 bg-surface-card ring-1 ring-ruby/15"
      : "border-border bg-surface-card/60 hover:border-ruby/20 hover:bg-surface-card",
  ]}
>
  <div class="flex flex-1 flex-col">
    <div class="flex items-start gap-4">
      {
        icon && (
          <img
            src={icon}
            alt={`${name} icon`}
            width={64}
            height={64}
            class="h-16 w-16 shrink-0 rounded-2xl bg-surface-raised object-contain ring-1 ring-border"
            loading="lazy"
          />
        )
      }
      <div class="min-w-0 flex-1">
        <div class="flex items-start justify-between gap-4">
          <div>
            <p class="text-xs font-semibold uppercase tracking-wider text-ruby">Side project</p>
            <h3 class="mt-2 text-xl font-semibold text-white sm:text-2xl">{name}</h3>
            <p class="mt-1 text-sm font-medium text-accent">{tagline}</p>
          </div>
          <span
            class="hidden shrink-0 rounded-lg bg-emerald-400/10 px-2.5 py-1 text-xs font-medium text-emerald-400 ring-1 ring-emerald-400/20 sm:inline"
          >
            Live
          </span>
        </div>
      </div>
    </div>

    <p class="mt-4 flex-1 text-sm leading-relaxed text-muted">{description}</p>

    <div class="mt-5">
      <p class="mb-2.5 text-xs font-semibold uppercase tracking-wider text-muted">Stack</p>
      <div class="flex flex-wrap gap-2">
        {stack.map((tech) => (
          <TechBadge name={tech} size="sm" />
        ))}
      </div>
    </div>
  </div>

  <div class="mt-6 flex flex-wrap gap-3 border-t border-border/60 pt-6">
    <a
      href={url}
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex items-center rounded-xl bg-ruby px-4 py-2 text-sm font-semibold text-white transition hover:bg-accent-hover"
    >
      Visit website →
    </a>
    {
      playStoreUrl && (
        <a
          href={playStoreUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 rounded-xl border border-border bg-surface-raised px-4 py-2 text-sm font-semibold text-white transition hover:border-ruby/30"
        >
          {
            playStoreIcon ? (
              <img src={playStoreIcon} alt="" width={20} height={20} class="h-5 w-5" loading="lazy" />
            ) : null
          }
          Google Play
        </a>
      )
    }
  </div>
</article>
